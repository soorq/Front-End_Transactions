const OtherComponent = ({ count }: { count: number }) => {
    return (
        <code className="bg-emerald-300">
            <pre>Your count is {count}</pre>
        </code>
    );
};

export default OtherComponent;
